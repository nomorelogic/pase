


TPaseBaseType
Valore base di un TPaseDataType
Questo tipo dovrebbe essere non visibile all'utente finale


TPaseDataType
Tipo dati utilizzabile dall'utente finale
A questo livello ci sono solo proprietà e metodi comuni a
- tipi scalari
- tipi non scalari (vettori, matrici, liste, collezioni, ec...)




Fatto
- AddUserType_Array + SetArrayBoundsByDim + History

Da fare
- AddUserType (è il caso di discriminare tra  usertype e "systemtype"?
